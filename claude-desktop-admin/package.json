{
  "name": "januaport-admin",
  "version": "0.1.0",
  "private": true,
  "description": "MCP bundle for Claude Desktop: stdio bridge to the JanuaPort admin MCP (/admin/mcp) with a bearer admin token.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "server/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.1"
  }
}
